# [プロジェクト名] — Claude/AI 開発コンテキスト

このリポジトリは、[サービスの一言説明]。
新しいセッションを開始した Claude／AI アシスタントは、まずこのファイルと `docs/` を読むこと。

## 着手前に必ず読むドキュメント（順番に）

1. `docs/product-context.md` — サービス全体像・確定方針
2. `docs/architecture.md` — 技術構成・DB 設計

> **新しい `docs/*.md` を追加したら、必ずこのリストに追記すること。追記しないと Claude はそのドキュメントの存在を知らない。**

## プロジェクト概要

[プロジェクトの目的・ターゲットユーザー・価値提案を記載]

## 確定方針（変更には必ずユーザー合意が必要）

| # | 方針 | 理由 |
|---|---|---|
| 1 | [確定方針を記載] | [理由] |

## ターゲットユーザー

- **第一（MVP）**: [ターゲットを具体的に]
- **拡張（v1.0 以降）**: [拡張ターゲット]
- **NOT**: [明示的に対象外とするユーザー]

## 技術スタック

- フロント: [技術スタック]
- バック: [技術スタック]
- ホスティング: [ホスティング先]
- DB: [DB 技術]
- 決済: [決済サービス（該当する場合）]

## 開発作業の指針

- ドキュメント（`docs/`）を真実の源とする。コードと矛盾が出たらユーザーに確認
- [プロジェクト固有のルールを追記]

### ドキュメント追加時のルール

新しい `docs/*.md` を追加したら、必ず CLAUDE.md の「着手前に必ず読むドキュメント」リストに追記すること。

## Issue 駆動開発（絶対ルール）

**すべてのタスクは GitHub issue から始める。コードより issue が先。**

### タスク着手フロー

```
0. 会話中に別要件が出たら即 issue 化（着手はしない）
1. issue 作成（/issue-pm create "タイトル"）
2. ブランチ作成（gh issue develop N でリンク）
3. 着手時にステータスを「作業中」へ
4. 実装計画を issue に記載してから着手
5. 実装完了後に PR 作成（Closes #N を必ず含める）
```

### ルール

1. issue なしのコミットは原則禁止
2. ブランチ名は `<type>/<issue番号>-<slug>` 形式（例: `feat/42-user-auth`）
3. PR タイトルは `<type>: <概要> (#N)` 形式
4. コミットメッセージに `#N` を含める

詳細: `.claude/skills/issue-pm/SKILL.md`

## TDD（絶対ルール）

**実装タスクはすべてテストファーストで進める。**

1. テストを先に書く（RED）→ 実装（GREEN）→ リファクタ（REFACTOR）
2. コミット前に `npm run test:coverage` を実行し、カバレッジ **80% 以上**を確認
3. コミット前に `/project-review` スキルで規約違反チェックを必ず行う

詳細: `.claude/skills/tdd/SKILL.md`

## プロジェクトスキル（`.claude/skills/`）

| スキル | 用途 |
|---|---|
| `issue-pm` | Issue 駆動開発 PM（issue 作成・ブランチ・PR 管理） |
| `issue-progress` | GitHub Projects 進捗管理 |
| `tdd` | TDD ワークフロー（Red→Green→Refactor） |
| `project-review` | コミット前チェックリスト（テスト・型・デザイン・セキュリティ） |
| `refactor` | リファクタゲート（重複・dead code・再利用・複雑度） |
| `qa-review` | テストシナリオが実運用に沿っているか QA 視点でレビュー |
| `po-review` | 機能・実装が現場の運用ニーズと合っているか PO 視点でレビュー |
| `design-system` | デザインシステム規約クイックリファレンス |
| `coverage-check` | カバレッジ確認・未テストパスへのテスト追加 |
| `config-audit` | Claude 設定ファイルの健全性チェック |
| [追加スキル] | [用途] |

> **実装完了後のレビューゲート**: 実装ファイル（`.ts`/`.tsx`/`.py`）を変更すると、
> Stop フックが `/project-review`・`/qa-review`・`/po-review`・`/refactor` を要求する。
> 各スキル完了後は `bash .claude/hooks/mark-review-passed.sh <スキル名>` で記録する
> （`touch` では記録できない＝レビュースキップを構造的に防止）。

## 関連リソース

- [関連ドキュメントへのリンク]
